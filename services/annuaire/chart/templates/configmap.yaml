apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "annuaire.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  APP_ENV: "dev"
  VERSION: "v2"
